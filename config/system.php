<?php

return [
    /*
    |--------------------------------------------------------------------------
    | System Token
    |--------------------------------------------------------------------------
    |
    | This token is used for system-level API access, typically from the frontend
    | for public pages that don't require user authentication.
    |
    */
    'token' => env('SYSTEM_API_TOKEN'),
];
